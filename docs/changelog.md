# Changelog

This documentation page mirrors the root `CHANGELOG.md` file.

See the root file for the canonical release history.

## Current status

- `CHANGELOG.md` in the repository root is the source of truth for release notes.
